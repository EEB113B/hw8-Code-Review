    def sum_of_smaller(self, now):
        self.root = BST(new)
        cur_new = self.root
        while cur_new != None:
            cur_new += cur.left  #將新的根節點加上左子樹的值
            if cur_new > cur.right: 
                cur_new += cur.right

#留言板